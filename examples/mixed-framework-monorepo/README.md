# Mixed Framework Monorepo Example

Illustrative consumer config showing one `test-station` run across:

- `jest`
- `node:test`

This example also demonstrates explicit workspace discovery where one workspace has no suites and therefore appears as a skipped package in the report.
